package net.virtalab.databazer.h2;

/**
 * Server modes defaines where Database will be stored if we use server mode)
 */
public enum ServerMode {
    /**
     * In-memory db
     */
    MEMORY,
    /**
     * DB stored in file
     */
    FILE

}
